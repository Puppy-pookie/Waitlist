# Dependencies
node_modules/

# Environment variables / secrets — never commit these
.env
.env.local
.env.*.local
.vercel

# Exported waitlist data (contains decrypted personal emails — never commit)
waitlist.csv
*.csv

# OS/editor cruft
.DS_Store
Thumbs.db
